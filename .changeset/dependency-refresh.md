---
'@dougefresh/pi-azure-foundry': patch
---

Refresh dev dependencies: `npm update` within existing ranges, plus `@types/node`
24 -> 26. No source changes; `@azure/identity`, the only runtime dependency, was
already current.
